// Public routes
const publicRoutes: string[] = [
    "/",
]

// These are routes for authentication
const authRoutes: string[] = [
    "/signin",
    "/signup",
    "/verify",
]

const SIGNIN_REDIRECT = "/profile"

export {publicRoutes, authRoutes, SIGNIN_REDIRECT}

