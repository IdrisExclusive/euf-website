export const metadata = {
  title: "Eagle Ummah foundation dashboard",
  description: "This is the dashboard of the eagle ummah foundation",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
